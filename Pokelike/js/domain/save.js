/**
 * @module domain/save
 * Account and run-save domain shell for the World Cup vertical slice.
 *
 * Stub only for P1-002. Save migration and settlement helpers are added in
 * later Phase 1 tasks.
 */
const DomainSave = Object.freeze({});

window.DomainSave = DomainSave;
