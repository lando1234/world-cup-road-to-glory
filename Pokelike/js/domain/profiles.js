/**
 * @module domain/profiles
 * Player profile catalog domain shell for the World Cup vertical slice.
 *
 * Stub only for P1-002. Catalog loading and profile lookup are added in later
 * Phase 1 tasks.
 */
const DomainProfiles = Object.freeze({});

window.DomainProfiles = DomainProfiles;
