/**
 * @module domain/styles
 * Style-system domain shell for the World Cup vertical slice.
 *
 * Stub only for P1-002. Style identifiers, labels, and chart data are added in
 * P1-003.
 */
const DomainStyles = Object.freeze({});

window.DomainStyles = DomainStyles;
